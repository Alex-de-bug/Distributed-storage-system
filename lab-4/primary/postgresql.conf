    # Настройки для репликации
    listen_addresses = '*'       # Слушать на всех интерфейсах
    wal_level = replica          # Уровень детализации WAL для репликации
    max_wal_senders = 3        # Макс. число процессов отправки WAL
    wal_keep_size = 64MB       # Минимальный размер WAL-файлов для хранения (замените wal_keep_segments в старых версиях)
    hot_standby = on             # Разрешить read-only запросы на реплике (для удобства, если primary станет standby)

    # Прочие стандартные настройки можно оставить по умолчанию или добавить по необходимости 